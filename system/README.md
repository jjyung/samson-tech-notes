# System

This directory holds the workflow scaffolding for the repo.

- `prompts/` contains reusable prompts for capture, drafting, and rewriting.
- `templates/` contains reusable markdown templates.

This is the operational layer around the wiki, not the wiki itself.
